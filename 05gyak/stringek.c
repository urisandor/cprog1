#include <stdio.h>
#include "prog1.h"
#include <string.h>

/*int my_strlen(string s)
{
    int i;

    for (i = 0; s[i] != '\0'; i++)
    {
        //ures
    }
    return i;
}*/

/*int my_strlen(string s)
{
    int i = 0;

    while (s[i] != '\0')
    {
        i++;
    }
    return i;
}
*/

/*int is_palindrome(string s)
{
    int i = 0;
    int j = strlen(s) - 1;
    
    while (i < j)
    {
        if (s[i] != s[j])
        {
            printf("Nem Palindrom");
            return 0;  // hamis
        }
        i++;
        j--;
    }
    
    return 1;  // igaz
}*/



int main() 
{

    //string s = "asd";
    //int hossz = my_strlen(s);
    //printf("A string hossza: %d\n", hossz);
    //string s = "asd";
    //printf("'%s' palindrom? %s\n", s, is_palindrome(s) ? "igen" : "nem");
    
    string s = "hello";
    char c = get_char(Karakter)

    return 0;
}